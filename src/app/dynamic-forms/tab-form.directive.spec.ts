import { TabFormDirective } from './tab-form.directive';

describe('TabFormDirective', () => {
  it('should create an instance', () => {
    const directive = new TabFormDirective();
    expect(directive).toBeTruthy();
  });
});
